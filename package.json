{
  "name": "token-list",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "rm -rf tokens tags.json && node scripts/sync.js",
    "test": "node scripts/test.js",
    "build": "node scripts/build.js"
  },
  "dependencies": {
    "isomorphic-fetch": "^3.0.0",
    "superstruct": "^0.15.3",
    "yaml": "^2.0.0-9"
  }
}
